<div class="sidebar">
    <div class="logo">
        <div class="logo-text">Imperial College</div>
        <a href="{{ siteurl }}/"><img alt="Imperial College Caving Club logo" src="{{ asset('assets/iclogowhite.svg', 'theme') }}"></a>
        <div class="logo-text">Caving Club</div>
        {% if user is not empty %}
        <div class="welcome">{{ user.displayname }}</div>
        {% endif %}
    </div>

    <div class="sidebar-content-box">
        <div class="sidebar-content-box">
            <div class="sidebar-item">
                {% if user is empty %}
                    <a href="{{ siteurl }}{{ config.get('general/branding/path') }}">Sign in</a>
                {% else %}
                    <a href="{{ siteurl }}{{ config.get('general/branding/path') }}">Admin</a>
                {% endif %}
            </div>
        </div>
    </div>
    {{ widgets('sidebar') }}
</div>
